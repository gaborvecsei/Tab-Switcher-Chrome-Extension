{
  "manifest_version": 3,
  "name": "Tab Switcher",
  "version": "1.0",
  "description": "Switch back and forth between the two most recently active tabs.",
  "permissions": ["tabs", "storage"],
  "background": {
    "service_worker": "background.js"
  },
  "commands": {
    "toggle-tabs": {
      "suggested_key": {
        "default": "Ctrl+Shift+X"
      },
      "description": "Switch to the last active tab"
    }
  }
}
